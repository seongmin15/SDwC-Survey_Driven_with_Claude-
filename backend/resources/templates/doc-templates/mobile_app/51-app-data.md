# App State & Data

> 이 문서는 프로젝트 시작 시 AI가 상세를 채웁니다.

## Data Model

<!-- AI:INIT: During initialization, define local data model:
     - Core entities and their relationships
     - Local storage schema (SQLite, Realm, Core Data, etc.)
     - Data types and validation rules
-->

## API Data Contract

<!-- AI:INIT: During initialization, define server sync:
     - Request/Response DTOs per endpoint
     - Mapping: API response → local model
     - Pagination strategy
     - Partial update handling
-->

## Offline Support

<!-- AI:INIT: During initialization, define offline behavior:
     - Which features work offline
     - Cache-first vs network-first per screen
     - Conflict resolution strategy (last-write-wins, merge, user-choice)
     - Sync queue for pending mutations
-->

## Authentication Flow

<!-- AI:INIT: During initialization, define auth:
     - Token storage (keychain, encrypted prefs)
     - Token refresh flow
     - Session expiry handling
     - Biometric authentication (if applicable)
-->

## Asset Management

<!-- AI:INIT: During initialization, define assets:
     - Image loading and caching strategy
     - File download/upload handling
     - Bundle size optimization
-->
